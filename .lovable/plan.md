
# Модернизация Главной Страницы WeddingTech UZ

## Обзор Проблем

После детального анализа выявлены следующие недостатки текущего дизайна:

| Проблема | Описание |
|----------|----------|
| **Устаревшая цветовая схема** | Sage Green (#9CAF88) слишком приглушённый, не создаёт премиального ощущения |
| **Однотипные изображения** | Статичные JPG без динамики, устаревший визуальный стиль |
| **Простые карточки** | Стандартный grid layout без современных Bento-элементов |
| **Монотонные анимации** | Однообразные hover-эффекты, отсутствие микро-взаимодействий |
| **Слабый Hero** | Нет depth layers, простой текст без split-reveal эффектов |

---

## Новая Цветовая Палитра 2025

**Текущие цвета (удаляем):**
- Sage Green: `hsl(95, 25%, 55%)` — слишком тусклый

**Новая премиальная палитра:**

```text
┌─────────────────────────────────────────────────────────────┐
│                    LUXE WEDDING PALETTE                     │
├─────────────────────────────────────────────────────────────┤
│  Primary:     Rose Gold      hsl(15, 60%, 65%)    #D4A373   │
│  Secondary:   Champagne      hsl(40, 45%, 88%)    #F5E6D3   │
│  Accent:      Deep Burgundy  hsl(345, 45%, 35%)   #7D2E46   │
│  Neutral:     Warm Ivory     hsl(40, 30%, 96%)    #FAF6F0   │
│  Highlight:   Soft Gold      hsl(45, 70%, 60%)    #D4AF37   │
│  Text:        Charcoal       hsl(220, 15%, 20%)   #2D3142   │
└─────────────────────────────────────────────────────────────┘
```

---

## План Реализации

### Фаза 1: Обновление Design System

**Файл: `src/index.css`**

1. Заменить CSS-переменные на новую палитру
2. Исправить структурную ошибку (sidebar переменные внутри `.gpu-accelerated`)
3. Добавить новые утилиты:
   - `.text-reveal` — побуквенное появление
   - `.glass-luxe` — премиальный glassmorphism
   - `.gradient-luxe` — rose gold градиенты
   - `.glow-gold` — золотое свечение

**Файл: `tailwind.config.ts`**
- Обновить цвета в конфигурации
- Добавить новые keyframes для современных анимаций

---

### Фаза 2: Hero Section — Полная Переработка

**Файл: `src/components/landing/Hero.tsx`**

**Новый дизайн:**

```text
┌─────────────────────────────────────────────────────────────┐
│  ╭──────────────────────────────╮                           │
│  │   🏆 #1 Wedding Platform    │  ← 3D floating badge      │
│  │      in Central Asia         │     с parallax depth      │
│  ╰──────────────────────────────╯                           │
│                                                             │
│  ████████████████████████████████                           │
│  █                              █                           │
│  █   Увидьте свою              █  ← Split-text reveal      │
│  █   МЕЧТУ О СВАДЬБЕ           █     letter-by-letter      │
│  █   ещё до её начала          █                           │
│  █                              █                           │
│  ████████████████████████████████                           │
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                   │
│  │  1500+   │  │   10K+   │  │   98%    │                   │
│  │ Vendors  │  │ Weddings │  │  Happy   │  ← 3D Tilt Cards │
│  │   ↗️     │  │    ↗️    │  │   ↗️     │                   │
│  └──────────┘  └──────────┘  └──────────┘                   │
│                                                             │
│  ╭─────────────────────────────────────╮                    │
│  │ ✨ Начать планирование  →          │  ← Morph Button    │
│  ╰─────────────────────────────────────╯                    │
└─────────────────────────────────────────────────────────────┘
```

**Ключевые изменения:**
- Новые высококачественные изображения с gradient overlay
- Split-text анимация заголовка (каждая буква появляется отдельно)
- 3D Stat Cards с mouse-tracking tilt эффектом
- Animated gradient background вместо статичного mesh
- Floating 3D badges с depth parallax

---

### Фаза 3: Features — Bento Grid Layout

**Файл: `src/components/landing/Features.tsx`**

**Новый Bento Grid:**

```text
┌─────────────────────────────────────────────────────────────┐
│                    BENTO GRID LAYOUT                        │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────────┐  ┌───────────┐  ┌───────────┐     │
│  │                      │  │           │  │           │     │
│  │    AI VISUALIZER     │  │  Invite   │  │ Payments  │     │
│  │     (БОЛЬШАЯ)        │  │  Creator  │  │   Safe    │     │
│  │                      │  │           │  │           │     │
│  │  [Video Preview]     │  ├───────────┤  ├───────────┤     │
│  │                      │  │           │  │           │     │
│  ├──────────────────────┤  │ Planner   │  │  Guests   │     │
│  │     Marketplace      │  │   Smart   │  │  Manage   │     │
│  │     1500+ vendors    │  │           │  │           │     │
│  └──────────────────────┘  └───────────┘  └───────────┘     │
└─────────────────────────────────────────────────────────────┘
```

**Улучшения:**
- Главная карточка (AI Visualizer) занимает 2 колонки
- Interactive hover с 3D tilt эффектом
- Gradient borders на hover
- Micro-animations для иконок
- Video preview для AI-функций

---

### Фаза 4: Новые UI Компоненты

**1. `src/components/landing/TextReveal.tsx`**
- Побуквенное появление с stagger эффектом
- Gradient mask reveal
- Spring physics для плавности

**2. `src/components/landing/TiltCard.tsx`**
- 3D перспектива на движение мыши
- Depth layers внутри карточки
- Glare/reflection эффект
- GPU-accelerated transforms

**3. `src/components/landing/GradientButton.tsx`**
- Animated mesh gradient background
- Magnetic attraction к курсору
- Ripple effect при клике
- Shimmer sweep анимация

**4. `src/components/landing/FloatingBadge.tsx`**
- Parallax depth на scroll
- Glassmorphism с blur
- Pulse glow анимация

---

### Фаза 5: AI Showcase — Переработка

**Файл: `src/components/landing/AIShowcase.tsx`**

**Изменения:**
- Заменить статичные изображения на gradient placeholders с иконками
- Добавить видео-демо для AI Visualizer
- Interactive demo preview
- Animated feature list с stagger
- 3D card rotation на scroll

---

### Фаза 6: Pricing & Stats — Luxe Дизайн

**Файл: `src/components/landing/Pricing.tsx`**
- Gold gradient для "Популярный" badge
- Glass cards с rose gold borders
- Animated checkmarks
- Hover glow effects

**Файл: `src/components/landing/Stats.tsx`**
- Изменить зелёный фон на rose gold gradient
- Circular progress rings вместо чисел
- Count-up с spring physics
- Floating particle decorations

---

### Фаза 7: CTA & Footer

**Файл: `src/components/landing/CTA.tsx`**
- Luxe gradient (rose gold → burgundy)
- 3D floating decorative elements
- Animated input focus states
- Premium button с gold shimmer

---

## Технические Детали

### Новые CSS Классы

```css
/* Luxe Glass Effect */
.glass-luxe {
  background: linear-gradient(135deg, 
    rgba(255, 255, 255, 0.15) 0%, 
    rgba(212, 163, 115, 0.1) 100%
  );
  backdrop-filter: blur(24px) saturate(180%);
  border: 1px solid rgba(212, 175, 55, 0.2);
}

/* Rose Gold Gradient */
.gradient-luxe {
  background: linear-gradient(135deg, 
    hsl(15, 60%, 65%) 0%, 
    hsl(45, 70%, 60%) 50%,
    hsl(15, 60%, 65%) 100%
  );
}

/* Gold Glow */
.glow-gold {
  box-shadow: 
    0 0 40px rgba(212, 175, 55, 0.3),
    0 0 80px rgba(212, 175, 55, 0.15);
}

/* Text Split Reveal */
.text-split-reveal span {
  display: inline-block;
  opacity: 0;
  animation: letter-reveal 0.5s ease forwards;
}
```

### Оптимизации Performance

1. **Lazy Loading**: Компоненты ниже fold загружаются через `React.lazy()`
2. **GPU Acceleration**: Все анимации используют `transform` и `opacity`
3. **Intersection Observer**: Анимации запускаются только в viewport
4. **Memoization**: Статические компоненты обёрнуты в `memo()`
5. **Reduced Motion**: Поддержка `prefers-reduced-motion`

---

## Порядок Файлов для Изменения

1. `src/index.css` — новая цветовая палитра и утилиты
2. `tailwind.config.ts` — конфигурация цветов
3. `src/components/landing/TextReveal.tsx` — новый компонент
4. `src/components/landing/TiltCard.tsx` — новый компонент
5. `src/components/landing/Hero.tsx` — полная переработка
6. `src/components/landing/Features.tsx` — Bento Grid
7. `src/components/landing/AIShowcase.tsx` — новые изображения
8. `src/components/landing/Pricing.tsx` — luxe стиль
9. `src/components/landing/Stats.tsx` — новый gradient
10. `src/components/landing/CTA.tsx` — premium дизайн
11. `src/components/landing/Header.tsx` — обновление цветов
12. `src/components/landing/HowItWorks.tsx` — согласование стиля

---

## Ожидаемый Результат

После внедрения:
- **Современный премиальный вид** — Rose Gold + Champagne палитра
- **Улучшенный UX** — плавные 3D анимации и micro-interactions
- **Высокая производительность** — GPU-accelerated, оптимизированные анимации
- **Уникальность** — Bento Grid и 3D tilt карточки
- **Consistency** — единый дизайн-язык на всех секциях
